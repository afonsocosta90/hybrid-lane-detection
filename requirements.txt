# Computer vision and math
opencv-python>=4.8.0
numpy>=1.24.0
scipy>=1.11.0

# DL backend: UFLDv1 inference (PyTorch)
ufld @ git+https://github.com/afonsocosta90/fast-lane-detection.git@main
torchvision

# torch is intentionally NOT listed here — pick the right wheel for your hardware:
#   AMD on Windows : pip install torch-directml
#   NVIDIA         : pip install torch --index-url https://download.pytorch.org/whl/cu121
#   CPU only       : pip install torch
